<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
                xmlns:tools="http://schemas.android.com/tools"
                android:orientation="vertical"
                android:layout_width="match_parent"
                android:layout_height="match_parent">
    <ImageView
        android:id="@+id/title_image_view"
        android:layout_width="match_parent"
        android:layout_height="100dp"
        android:scaleType="centerCrop"
        android:paddingBottom="5dp"/>

    <TextView
        android:id="@+id/myImageViewText"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignLeft="@+id/title_image_view"
        android:layout_alignTop="@+id/title_image_view"
        android:layout_alignRight="@+id/title_image_view"
        android:layout_alignBottom="@+id/title_image_view"
        android:layout_margin="1dp"
        android:gravity="center"
        android:text="Hello"
        android:textStyle="bold"
        android:textSize="30dp"
        android:textColor="#ffcc00" />
</RelativeLayout>